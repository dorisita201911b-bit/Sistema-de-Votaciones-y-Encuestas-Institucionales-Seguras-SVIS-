# Configuracion de la base de datos (modo mysql).
# Las variables de entorno EVENTOS_DB_URL / EVENTOS_DB_USER / EVENTOS_DB_PASSWORD
# tienen prioridad sobre estos valores.

db.url=jdbc:mysql://localhost:3306/eventos_qr?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
db.user=root
db.password=
db.poolSize=5
